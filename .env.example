# Backend URLs Configuration
# Primary backend (Railway)
REACT_APP_PRIMARY_BACKEND=https://task-recommendation-backend-production.up.railway.app

# Fallback backend (Render - optional)
REACT_APP_FALLBACK_BACKEND=https://task-recommendation-backend.onrender.com

# API timeout in milliseconds
REACT_APP_API_TIMEOUT=8000
